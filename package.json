{
  "name": "element-box",
  "version": "0.1.0",
  "description": "box trial",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "cd client && npm install && bower install && gulp build && cd ../server && npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gulp": "^3.8.8",
    "rimraf": "^2.2.8",
    "run-sequence": "^1.0.1",
    "shelljs": "^0.3.0"
  },
  "devDependencies": {
    "gulp-express": "^0.1.0"
  }
}
